services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: med_bnb
      POSTGRES_PASSWORD: med_bnb_password
      POSTGRES_DB: med_bnb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data


volumes:
  pgdata: